#ifndef __HOUND_TYPES_H__
#define __HOUND_TYPES_H__

enum class ASSET_TYPE
{
	NONE = 0,
	TEXTURE,
	SPRITEFRAME,
	ANIMATION,
	MAX_VALUE
};

enum class ENTITY_TYPE
{
	NONE = 0,
	HOUND,
	ENEMY,
	MAX_VALUE
};

enum class BODY_TYPE
{
	NONE = 0,
	BASIC,
	MAX_VALUE
};

enum class ARMOR_TYPE
{
	NONE = 0,
	ENEGY_SHIELD,
	REACTIVE_ARMOR,
	CERAMIC_ARMOR,
	MAX_VALUE
};

enum class ENGINE_TYPE
{
	NONE = 0,
	BASIC,
	MAX_VALUE
};

enum class WINGMAN_TYPE
{
	NONE = 0,
	BASIC,
	MAX_VALUE
};

enum class ENEMY_TYPE
{
	NONE = 0,
	FIGHTER_BEE,
	FRIGATE_PUMA,
	CARRIER_TIGER,
	MAX_VALUE
};

enum class WEAPON_TYPE
{
	NONE = 0,
	CANNON,
	MAX_VALUE
};

enum class BARREL_TYPE
{
	NONE = 0,
	BULLET,
	LASER,
	MISSILE,
	MAX_VALUE
};

enum class PROJECTILE_TYPE
{
	NONE = 0,
	BULLET_NORMAL,
	LASER_NORMAL,
	MISSILE_NORMAL,
	MAX_VALUE
};

enum class MOVEMENT_TYPE
{
	NONE = 0,
	DISPLACEMENT,
	ROTATION,
	STAY,
	MAX_VALUE
};

enum class STATE_TYPE
{
	NONE = 0,
	MOVE,
	ENTRY,
	BATTLE_PHASE,
	TRANSFORM,
	LEAVE,
	BATTLE_END,
	DEAD,
	MAX_VALUE
};

enum class STATE_MACHINE_EVENT
{
	NONE = 0,
	START,
	MOVE_FINISHED,
	ENTRY_FINISHED,
	BATTLE_PHASE_FINISHED,
	TRANSFORM_FINISHED,
	LEAVE_FINISHED,
	DESTROYED,
	HOUND_DEAD,
	LEVEL_QUIT,
	VICTORY,
	MAX_VALUE
};

#endif //__HOUND_TYPES_H__